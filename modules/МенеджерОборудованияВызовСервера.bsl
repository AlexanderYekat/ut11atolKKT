#Область ВстариваниеВКонфуУТ11
функция ПолучитьИмяВнешнейОбработки(Адрес) экспорт
	возврат ВнешниеОбработки.Подключить(Адрес);
конецФункции //ПолучитьИмяВнешнейОбработки

функция ОбработкаПрисутсвуетВКонфигурации(имяОбработки) экспорт
	рез = ИСТИНА;
	Если Метаданные.НайтиПоПолномуИмени("Обработка."+имяОбработки) =НЕОПРЕДЕЛЕНО тогда
		рез = ЛОЖЬ;
	конецЕсли;
	возврат рез;
конецФункции //ОбработкаПрисутсвуетВКонфигурации
#КонецОбласти 
